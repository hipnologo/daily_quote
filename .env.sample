# API Key for Daily Quote Generation
# Get your API key from https://api-ninjas.com/
API_NINJAS_KEY=your_api_key_here

# Backend API Configuration
# Database URL (defaults to SQLite for local development)
# Example PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./daily_quote_admin.db

# Security Configuration
# Secret key for JWT token generation (Change this in production!)
# You can generate a secure key using: openssl rand -hex 32
SECRET_KEY=your-super-secret-key-change-in-production

# Optional: JWT Settings
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256
