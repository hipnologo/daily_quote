apiVersion: v1
kind: Secret
metadata:
  name: daily-quote-secrets
  namespace: daily-quote
  labels:
    app: daily-quote
type: Opaque
stringData:
  # JWT Secret Key - CHANGE THIS IN PRODUCTION!
  JWT_SECRET_KEY: "your-super-secret-key-change-in-production"
  # Default admin password
  ADMIN_PASSWORD: "admin123"
